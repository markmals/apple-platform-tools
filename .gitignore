# macOS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
*.iml
xcuserdata/
*.xcodeproj/
*.xcworkspace/

# Swift / SwiftPM build outputs
.build/
.swiftpm/
DerivedData/
Package.resolved.bak

# flexscope signed artifacts — NEVER distribute (see specs/ARCHITECTURE.md → Security)
# Build outputs are regenerated by `mise run build` + `mise run sign`.
dist/
*.dylib
!third_party/**/*.dylib
*.framework/

# Sockets / runtime scratch the tool creates
/tmp/flexscope-*.sock
*.pid

# Logs and temp
*.log
*.tmp

# Tests / coverage
coverage/
.nyc_output/
