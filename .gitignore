# macOS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
*.iml
xcuserdata/
*.xcodeproj/
*.xcworkspace/

# Swift / SwiftPM build outputs
.build/
.swiftpm/
DerivedData/
Package.resolved.bak

# Runtime-cluster signed artifacts (the uitool boot dylib + FLEX-mac framework)
# — NEVER distribute (see Specs/ARCHITECTURE.md → "Dual-use & safety posture").
# Regenerated by the build/sign script; they are an attack tool elsewhere.
dist/
*.dylib
!third_party/**/*.dylib
*.framework/

# Sockets / runtime scratch the runtime cluster creates
/tmp/uitool-*.sock
*.pid

# Logs and temp
*.log
*.tmp
